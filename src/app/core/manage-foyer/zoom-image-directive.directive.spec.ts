import { ZoomImageDirectiveDirective } from './zoom-image-directive.directive';

describe('ZoomImageDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomImageDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
