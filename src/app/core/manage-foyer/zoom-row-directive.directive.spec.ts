import { ZoomRowDirectiveDirective } from './zoom-row-directive.directive';

describe('ZoomRowDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomRowDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
