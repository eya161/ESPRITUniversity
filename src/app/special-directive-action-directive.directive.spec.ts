import { SpecialDirectiveActionDirectiveDirective } from './special-directive-action-directive.directive';

describe('SpecialDirectiveActionDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SpecialDirectiveActionDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
